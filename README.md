 # AI Customer Persona Generator

This is a Python web application built using Flask that automatically generates highly detailed target customer personas using the Google Gemini API.

## Setup Instructions

1. Install required packages:
   `bash
   pip install -r requirements.txt